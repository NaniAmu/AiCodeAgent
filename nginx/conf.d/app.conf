server {
    listen 80;
    server_name _;  # Replace with your domain: api.diginest.com
    
    # Certbot challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Redirect HTTP to HTTPS (enable after SSL setup)
    # location / {
    #     return 301 https://$server_name$request_uri;
    # }
    
    # Temporary: Proxy to app for HTTP
    location / {
        proxy_pass http://app:8080;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS Configuration (enable after SSL certificate obtained)
# server {
#     listen 443 ssl http2;
#     server_name api.diginest.com;  # Replace with your domain
#     
#     # SSL Certificates
#     ssl_certificate /etc/letsencrypt/live/api.diginest.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.diginest.com/privkey.pem;
#     
#     # SSL Configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     
#     # Health check endpoint (no rate limiting)
#     location /actuator/health {
#         proxy_pass http://app:8080/actuator/health;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         access_log off;
#     }
#     
#     # API endpoints with rate limiting
#     location /api/ {
#         limit_req zone=api burst=20 nodelay;
#         
#         # Stricter rate limit for auth endpoints
#         location /api/auth/login {
#             limit_req zone=login burst=5 nodelay;
#             proxy_pass http://app:8080;
#             proxy_http_version 1.1;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#         
#         proxy_pass http://app:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # Timeouts
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#     
#     # Block access to actuator endpoints except health
#     location /actuator {
#         deny all;
#         return 404;
#     }
# }
